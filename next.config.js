/** @type {import('next').NextConfig} */
const nextConfig = {
  // Keep only essential configs, remove deployment-related ones like:
  // output: 'export',
  // basePath: '',
  // images: { unoptimized: true },
}

module.exports = nextConfig 